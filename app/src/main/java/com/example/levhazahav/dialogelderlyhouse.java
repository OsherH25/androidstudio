package com.example.levhazahav;

import android.app.AlertDialog;
import android.app.Dialog;
import android.os.Bundle;
import android.view.LayoutInflater;
import android.view.View;
import android.widget.TextView;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.appcompat.app.AppCompatDialogFragment;

//public class dialogelderlyhouse extends AppCompatDialogFragment {

  //  TextView tvLocation, tvPhone, tvAvBedsNum, tvRating, btnSendMsg;


   // @NonNull
   // @Override
  //  public Dialog onCreateDialog(@Nullable Bundle savedInstanceState) {

        //  tvLocation = findViewById(R.id.tvLocation);
        // tvPhone = findViewById(R.id.tvPhone);
        //  tvAvBedsNum = findViewById(R.id.tvAvBedsNum);
        // tvRating = findViewById(R.id.tvRating);
        // btnSendMsg = findViewById(R.id.btnSendMsg);


     //   AlertDialog.Builder builder = new AlertDialog.Builder(getActivity());

      //  LayoutInflater inflater = getActivity().getLayoutInflater();
      //  View view = inflater.inflate(R.layout.elderlyhouse_dialog_layout, null);

        //builder.setView(view).setTitle("");



